namespace DataMaker
{
    /// <summary>
    /// Represents an address with street, city, and state.
    /// </summary>
    public record AddressData(string StreetAddress, string City, string State);
}
